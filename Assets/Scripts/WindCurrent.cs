using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WindCurrent : MonoBehaviour
{
    //[SerializeField, Range(0, 5f)] private float windSpeed = 2;

    // Update is called once per frame
    void Update()
    {
        
    }
}
